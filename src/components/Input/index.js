import { InputContainer } from "./styles";

const Input = ({value}) => {
    return (
      <InputContainer>
        <Input disabled />
      </InputContainer>
    );
  }
  
  export default Input;
  